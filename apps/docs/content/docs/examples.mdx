---
title: Examples
description: Real-world examples and use cases for Media SDK
---

import { Tabs, Tab } from 'fumadocs-ui/components/tabs';
import { Callout } from 'fumadocs-ui/components/callout';

# Examples

Practical examples showing how to use Media SDK for real-world video creation scenarios.

## TikTok Videos

Create viral TikTok-style content with mobile-optimized formatting.

```typescript
import { Timeline } from '@jamesaphoenix/media-sdk';

const tiktokVideo = new Timeline()
  // Mobile-first 9:16 aspect ratio
  .setAspectRatio('9:16')
  .setResolution(1080, 1920)
  .setDuration(15)
  
  // Main content video
  .addVideo('content.mp4')
  
  // Background music
  .addAudio('viral-beat.mp3', { 
    volume: 0.6,
    loop: true 
  })
  
  // Hook text at the beginning
  .addText('Wait for it... 👀', {
    position: { x: 'center', y: '15%' },
    style: {
      fontSize: 48,
      color: '#ffffff',
      fontFamily: 'Arial Black',
      strokeColor: '#000000',
      strokeWidth: 3
    },
    startTime: 0,
    duration: 3
  })
  
  // Payoff text
  .addText('OMG!! 😱🔥', {
    position: { x: 'center', y: '15%' },
    style: {
      fontSize: 64,
      color: '#ff0066',
      fontFamily: 'Arial Black',
      strokeColor: '#ffffff',
      strokeWidth: 2
    },
    startTime: 8,
    duration: 2
  })
  
  // Call to action
  .addText('Follow for more! ✨', {
    position: { x: 'center', y: '85%' },
    style: {
      fontSize: 32,
      color: '#ffffff',
      fontFamily: 'Arial Bold'
    },
    startTime: 12,
    duration: 3
  });

const command = tiktokVideo.getCommand('tiktok-viral.mp4');
```

<Callout type="info">
  **TikTok Tips**: Use high-contrast colors, keep text large for mobile viewing, and structure content as Hook → Payoff → Call-to-Action.
</Callout>

## YouTube Gaming Highlights

Create epic gaming highlight reels optimized for YouTube.

```typescript
const gamingHighlights = new Timeline()
  // Desktop 16:9 format
  .setAspectRatio('16:9')
  .setResolution(1920, 1080)
  .setDuration(60)
  
  // Main gameplay footage
  .addVideo('gameplay-main.mp4')
  
  // Epic background music
  .addAudio('epic-gaming-music.mp3', { 
    volume: 0.7,
    fadeIn: 2,
    fadeOut: 3
  })
  
  // Webcam overlay (picture-in-picture)
  .addVideo('webcam-reaction.mp4', {
    position: { x: '75%', y: '75%' },
    scale: 0.25,
    startTime: 5,
    duration: 15
  })
  
  // Title card
  .addText('INSANE CLUTCH! 🔥', {
    position: { x: 'center', y: '20%' },
    style: {
      fontSize: 72,
      color: '#ff6600',
      fontFamily: 'Impact',
      strokeColor: '#000000',
      strokeWidth: 4
    },
    startTime: 0,
    duration: 4
  })
  
  // Subscribe reminder
  .addText('SMASH THAT SUBSCRIBE! 👆', {
    position: { x: 'center', y: '90%' },
    style: {
      fontSize: 32,
      color: '#ff0000',
      fontFamily: 'Arial Bold'
    },
    startTime: 50,
    duration: 10
  });
```

## Podcast Clips

Transform long-form podcast content into shareable social media clips.

```typescript
const podcastClip = new Timeline()
  // Square format for social media
  .setResolution(1080, 1080)
  .setDuration(45)
  
  // Background video/image
  .addVideo('podcast-background.mp4')
  
  // Main audio content
  .addAudio('podcast-segment.mp3', {
    volume: 1.0
  })
  
  // Podcast branding
  .addImage('podcast-logo.png', {
    position: { x: 'center', y: '15%' },
    scale: 0.3,
    duration: 45
  })
  
  // Quote text overlay
  .addText('"The future of AI is\\nnot replacing humans,\\nbut empowering them."', {
    position: { x: 'center', y: 'center' },
    style: {
      fontSize: 36,
      color: '#ffffff',
      fontFamily: 'Georgia',
      textAlign: 'center',
      lineHeight: 1.4,
      backgroundColor: 'rgba(0,0,0,0.7)',
      padding: 20,
      borderRadius: 15
    },
    startTime: 5,
    duration: 30
  })
  
  // Speaker attribution
  .addText('Dr. Sarah Chen\\nAI Research Director', {
    position: { x: 'center', y: '75%' },
    style: {
      fontSize: 24,
      color: '#cccccc',
      fontFamily: 'Arial',
      textAlign: 'center'
    },
    startTime: 10,
    duration: 25
  });
```

## Instagram Stories

Create engaging Instagram Stories with automated formatting.

```typescript
const instagramStory = new Timeline()
  .setAspectRatio('9:16')
  .setResolution(1080, 1920)
  .setDuration(10)
  
  // Background gradient or video
  .addVideo('story-background.mp4')
  
  // Main image or video content
  .addImage('product-photo.jpg', {
    position: { x: 'center', y: 'center' },
    scale: 0.8
  })
  
  // Brand logo
  .addImage('brand-logo.png', {
    position: { x: '85%', y: '15%' },
    scale: 0.15,
    duration: 10
  })
  
  // Story text
  .addText('New Product\\nLaunching Soon! 🚀', {
    position: { x: 'center', y: '80%' },
    style: {
      fontSize: 40,
      color: '#ffffff',
      fontFamily: 'Arial Bold',
      textAlign: 'center',
      strokeColor: '#000000',
      strokeWidth: 2
    },
    startTime: 2,
    duration: 8
  });
```

## Multi-Language Captions

Create videos with multiple language tracks.

```typescript
import { Timeline, MultiCaptionEngine } from '@jamesaphoenix/media-sdk';

// Create caption engine
const captionEngine = new MultiCaptionEngine();

// Set global styling
captionEngine.setGlobalDefaults({
  fontFamily: 'Arial Bold',
  fontSize: 36,
  color: '#ffffff',
  strokeColor: '#000000',
  strokeWidth: 2
});

// Create language tracks
const englishTrack = captionEngine.createTrack('en', 'English');
const spanishTrack = captionEngine.createTrack('es', 'Español');

// Add English captions
captionEngine.addCaption(englishTrack.id, {
  text: 'Welcome to our product demo!',
  startTime: 0,
  endTime: 3
});

captionEngine.addCaption(englishTrack.id, {
  text: 'This revolutionary feature will change everything.',
  startTime: 3,
  endTime: 7
});

// Add Spanish captions
captionEngine.addCaption(spanishTrack.id, {
  text: '¡Bienvenido a nuestra demo del producto!',
  startTime: 0,
  endTime: 3
});

captionEngine.addCaption(spanishTrack.id, {
  text: 'Esta característica revolucionaria lo cambiará todo.',
  startTime: 3,
  endTime: 7
});

// Create timeline with captions
const timeline = new Timeline()
  .addVideo('product-demo.mp4')
  .setDuration(10);

// In a full implementation, you would integrate the caption engine
// with the timeline to generate appropriate subtitle filters
```

## Automated Marketing Videos

Create template-based marketing videos with dynamic content.

```typescript
function createMarketingVideo(productName: string, price: string, imageUrl: string) {
  return new Timeline()
    .setAspectRatio('1:1')
    .setResolution(1080, 1080)
    .setDuration(8)
    
    // Background
    .addVideo('marketing-template.mp4')
    
    // Product image
    .addImage(imageUrl, {
      position: { x: 'center', y: '40%' },
      scale: 0.6,
      startTime: 1,
      duration: 6
    })
    
    // Product name
    .addText(productName, {
      position: { x: 'center', y: '70%' },
      style: {
        fontSize: 48,
        color: '#333333',
        fontFamily: 'Arial Bold',
        textAlign: 'center'
      },
      startTime: 2,
      duration: 5
    })
    
    // Price
    .addText(`Only ${price}!`, {
      position: { x: 'center', y: '80%' },
      style: {
        fontSize: 36,
        color: '#ff0066',
        fontFamily: 'Arial Bold',
        textAlign: 'center'
      },
      startTime: 4,
      duration: 4
    })
    
    // Call to action
    .addText('Shop Now! 🛒', {
      position: { x: 'center', y: '90%' },
      style: {
        fontSize: 32,
        color: '#ffffff',
        fontFamily: 'Arial Bold',
        backgroundColor: '#ff0066',
        padding: 10,
        borderRadius: 20
      },
      startTime: 6,
      duration: 2
    });
}

// Generate multiple product videos
const products = [
  { name: 'Wireless Headphones', price: '$99', image: 'headphones.jpg' },
  { name: 'Smart Watch', price: '$199', image: 'watch.jpg' },
  { name: 'Phone Case', price: '$29', image: 'case.jpg' }
];

products.forEach(product => {
  const timeline = createMarketingVideo(product.name, product.price, product.image);
  const command = timeline.getCommand(`${product.name.toLowerCase().replace(' ', '-')}.mp4`);
  console.log(command);
});
```

## Live Streaming Overlays

Create dynamic overlays for live streaming.

```typescript
const streamOverlay = new Timeline()
  .setResolution(1920, 1080)
  .setDuration(60)
  
  // Transparent background for overlay
  .addVideo('transparent-background.mov')
  
  // Stream title
  .addText('LIVE: Building the Future', {
    position: { x: 'center', y: '10%' },
    style: {
      fontSize: 36,
      color: '#ffffff',
      fontFamily: 'Arial Bold',
      backgroundColor: 'rgba(0,0,0,0.8)',
      padding: 15,
      borderRadius: 10
    },
    duration: 60
  })
  
  // Social media handles
  .addText('@username', {
    position: { x: '90%', y: '90%' },
    style: {
      fontSize: 24,
      color: '#ffffff',
      fontFamily: 'Arial'
    },
    duration: 60
  })
  
  // Live indicator
  .addText('🔴 LIVE', {
    position: { x: '10%', y: '10%' },
    style: {
      fontSize: 28,
      color: '#ff0000',
      fontFamily: 'Arial Bold'
    },
    duration: 60
  });
```

## Batch Processing

Process multiple videos with the same template.

```typescript
import { promises as fs } from 'fs';

async function batchProcessVideos(inputDir: string, outputDir: string) {
  const files = await fs.readdir(inputDir);
  const videoFiles = files.filter(file => file.endsWith('.mp4'));
  
  for (const file of videoFiles) {
    const inputPath = `${inputDir}/${file}`;
    const outputPath = `${outputDir}/processed-${file}`;
    
    const timeline = new Timeline()
      .addVideo(inputPath)
      
      // Add consistent branding
      .addImage('brand-logo.png', {
        position: { x: '85%', y: '15%' },
        scale: 0.2,
        duration: 'full'
      })
      
      // Add intro text
      .addText('Branded Content', {
        position: { x: 'center', y: '90%' },
        style: {
          fontSize: 32,
          color: '#ffffff',
          fontFamily: 'Arial Bold',
          backgroundColor: 'rgba(0,0,0,0.7)',
          padding: 10
        },
        startTime: 0,
        duration: 3
      });
    
    const command = timeline.getCommand(outputPath, {
      quality: 'high',
      codec: 'h264'
    });
    
    console.log(`Processing: ${file}`);
    console.log(`Command: ${command}`);
    
    // Execute command using your preferred method
    // await executeFFmpeg(command);
  }
}

// Usage
// await batchProcessVideos('./input-videos', './output-videos');
```

<Callout type="tip">
  **Performance Tip**: For batch processing, consider using the `FFmpegExecutor` with parallel processing limits to avoid overwhelming your system.
</Callout>

## Green Screen Memes

Create viral memes by removing green screens and replacing backgrounds with funny content.

```typescript
import { Timeline, PictureInPicture } from '@jamesaphoenix/media-sdk';

// Basic green screen meme
const timeline = Timeline.create()
  .addVideo('explosion-background.mp4')
  .setAspectRatio('9:16'); // TikTok format

const meme = PictureInPicture.addWithChromaKey(timeline, 'person-greenscreen.mp4', {
  position: 'center',
  scale: 0.5,
  chromaKey: '#00FF00',
  chromaSimilarity: 0.4,
  chromaBlend: 0.1,
  animation: 'zoom-in'
});

// Add meme text
const withText = meme
  .addText('When you realize', {
    position: { x: 'center', y: '10%' },
    style: {
      fontSize: 48,
      color: '#ffffff',
      fontFamily: 'Impact',
      strokeColor: '#000000',
      strokeWidth: 3
    },
    duration: 2
  })
  .addText('IT\'S MONDAY', {
    position: { x: 'center', y: '85%' },
    style: {
      fontSize: 64,
      color: '#ff0000',
      fontFamily: 'Impact',
      strokeColor: '#000000',
      strokeWidth: 4
    },
    startTime: 2,
    duration: 3
  });
```

### Multi-Layer Reaction Memes

```typescript
// Multiple people reacting
let meme = timeline;

// Add shocked person
meme = PictureInPicture.addWithChromaKey(meme, 'person1-shocked.mp4', {
  position: 'custom',
  customPosition: { x: '20%', y: '50%' },
  scale: 0.3,
  chromaKey: '#00FF00',
  animation: 'slide-in',
  animationDelay: 0.5
});

// Add laughing person
meme = PictureInPicture.addWithChromaKey(meme, 'person2-laughing.mp4', {
  position: 'custom',
  customPosition: { x: '70%', y: '40%' },
  scale: 0.35,
  chromaKey: '#00FF00',
  animation: 'slide-in',
  animationDelay: 1.0,
  flipHorizontal: true // Face the other person
});
```

### Gaming Streamer Overlay

```typescript
// Game footage background
const timeline = Timeline.create()
  .addVideo('epic-fail-gameplay.mp4')
  .setAspectRatio('16:9');

// Add streamer with different reactions
const calmReaction = PictureInPicture.addWithChromaKey(timeline, 'streamer-calm.mp4', {
  position: 'bottom-left',
  scale: 0.25,
  chromaKey: '#00FF00',
  borderRadius: 999, // Circle webcam
  startTime: 0,
  endTime: 5
});

const rageReaction = PictureInPicture.addWithChromaKey(calmReaction, 'streamer-rage.mp4', {
  position: 'bottom-left',
  scale: 0.35,
  chromaKey: '#00FF00',
  borderRadius: 999,
  borderWidth: 5,
  borderColor: '#ff0000',
  shadow: {
    blur: 10,
    color: 'red',
    offsetX: 0,
    offsetY: 0
  },
  startTime: 10,
  endTime: 15
});
```

### Weather Reporter Meme

```typescript
const weatherMeme = Timeline.create()
  .addVideo('weather-chaos-background.mp4')
  .setAspectRatio('16:9');

// Add weather person
const withReporter = PictureInPicture.addWithChromaKey(weatherMeme, 'person-pointing.mp4', {
  position: 'custom',
  customPosition: { x: '25%', y: '50%' },
  scale: 0.7,
  chromaKey: '#00FF00',
  audioMix: 'full' // Keep their audio
});

// Add weather graphics
const complete = withReporter
  .addImage('weather-map.png', {
    position: { x: '70%', y: '30%' },
    scale: 0.4,
    opacity: 0.9
  })
  .addText('TODAY\'S FORECAST: CHAOS', {
    position: { x: '70%', y: '80%' },
    style: {
      fontSize: 32,
      color: '#ffff00',
      backgroundColor: '#000000',
      padding: 10
    }
  });
```

<Callout type="tip">
  **Pro Tips for Green Screen Memes:**
  - Use `chromaSimilarity` 0.3-0.5 for best results
  - Add `chromaBlend` 0.1-0.2 for smoother edges
  - Good lighting on green screen is crucial
  - Consider `flipHorizontal` for interaction scenes
  - Layer multiple reactions for comedy gold
</Callout>

## Next Steps

- [🚀 Advanced Features](/docs/advanced) - Self-healing, vision integration
- [🧪 Testing](/docs/testing) - Quality assurance and validation
- [📖 API Reference](/docs/api) - Complete method documentation